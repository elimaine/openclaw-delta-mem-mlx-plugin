{
  "id": "delta-mem-mlx",
  "name": "Delta-Mem MLX",
  "version": "0.1.1",
  "description": "Apple Silicon helper plugin for connecting OpenClaw to a local delta-mem MLX sidecar.",
  "enabledByDefaultOnPlatforms": ["darwin"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "sidecarBaseUrl": {
        "type": "string",
        "default": "http://127.0.0.1:8765",
        "description": "Reachable base URL for the running delta-mem sidecar, without the /v1 suffix. Set this to whatever route OpenClaw has to the sidecar."
      },
      "modelId": {
        "type": "string",
        "default": "delta-mem-qwen3-4b-mlx",
        "description": "Model id exposed by the sidecar."
      },
      "sessionKeyTemplate": {
        "type": "string",
        "default": "agent:<agent-id>:<session-id>",
        "description": "Template for X-OpenClaw-Session-Key."
      },
      "attentionStateEnabled": {
        "type": "boolean",
        "default": true,
        "description": "When true, request QMD attention-state lookup before each outgoing message."
      },
      "attentionAdapter": {
        "type": "string",
        "default": "qmd",
        "description": "Attention lookup adapter name."
      },
      "attentionMode": {
        "type": "string",
        "default": "vsearch",
        "description": "Attention lookup mode. Default is QMD vector search."
      },
      "attentionQuerySource": {
        "type": "string",
        "default": "outgoing-message",
        "description": "Message text OpenClaw should use as the QMD lookup query."
      },
      "attentionLimit": {
        "type": "number",
        "default": 6,
        "description": "Maximum retrieved attention snippets to pass to the sidecar."
      },
      "sidecarRoot": {
        "type": "string",
        "description": "Optional existing checkout/install path for the delta-mem sidecar."
      },
      "managedInstallRoot": {
        "type": "string",
        "description": "Optional directory where the helper installer cloned the sidecar."
      },
      "installMode": {
        "type": "string",
        "enum": ["external", "managed"],
        "default": "external",
        "description": "external means the user manages the sidecar; managed means this plugin's helper scripts manage it."
      },
      "allowManagedDownloads": {
        "type": "boolean",
        "default": false,
        "description": "When true, setup helpers may download public Hugging Face model artifacts after explicit user confirmation."
      }
    }
  },
  "contracts": {
    "tools": [
      "delta_mem_mlx_status",
      "delta_mem_mlx_provider_config"
    ]
  },
  "toolMetadata": {
    "delta_mem_mlx_status": {
      "optional": true
    },
    "delta_mem_mlx_provider_config": {
      "optional": true
    }
  },
  "uiHints": {
    "sidecarBaseUrl": {
      "label": "Sidecar base URL",
      "placeholder": "http://127.0.0.1:8765"
    },
    "installMode": {
      "label": "Install mode"
    },
    "allowManagedDownloads": {
      "label": "Allow managed public model downloads"
    }
  }
}
